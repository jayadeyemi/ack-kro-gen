# Graph definitions now rely on config defaults for placeholder values.
# Only service and version are required here.

graphs:
  - service: s3
    version: "1.1.1"

  - service: ec2
    version: "1.7.0"

  - service: rds
    version: "1.6.2"
